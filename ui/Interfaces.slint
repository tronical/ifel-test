import { Device } from "DeviceInterface.slint";
import { Call } from "CallInterface.slint";

export { Device, Call}
export { 
    Device          as  DeviceInterface, 
    Call            as  CallInterface,
}